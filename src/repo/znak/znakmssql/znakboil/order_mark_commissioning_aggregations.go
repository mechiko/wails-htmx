// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package znakboil

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// OrderMarkCommissioningAggregation is an object representing the database table.
type OrderMarkCommissioningAggregation struct {
	ID                       int         `boil:"id" json:"id" toml:"id" yaml:"id"`
	IDOrderMarkCommissioning null.Int    `boil:"id_order_mark_commissioning" json:"id_order_mark_commissioning,omitempty" toml:"id_order_mark_commissioning" yaml:"id_order_mark_commissioning,omitempty"`
	IDOrderMarkAggregation   null.Int    `boil:"id_order_mark_aggregation" json:"id_order_mark_aggregation,omitempty" toml:"id_order_mark_aggregation" yaml:"id_order_mark_aggregation,omitempty"`
	Code                     null.String `boil:"code" json:"code,omitempty" toml:"code" yaml:"code,omitempty"`
	Status                   null.String `boil:"status" json:"status,omitempty" toml:"status" yaml:"status,omitempty"`

	R *orderMarkCommissioningAggregationR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L orderMarkCommissioningAggregationL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var OrderMarkCommissioningAggregationColumns = struct {
	ID                       string
	IDOrderMarkCommissioning string
	IDOrderMarkAggregation   string
	Code                     string
	Status                   string
}{
	ID:                       "id",
	IDOrderMarkCommissioning: "id_order_mark_commissioning",
	IDOrderMarkAggregation:   "id_order_mark_aggregation",
	Code:                     "code",
	Status:                   "status",
}

var OrderMarkCommissioningAggregationTableColumns = struct {
	ID                       string
	IDOrderMarkCommissioning string
	IDOrderMarkAggregation   string
	Code                     string
	Status                   string
}{
	ID:                       "order_mark_commissioning_aggregations.id",
	IDOrderMarkCommissioning: "order_mark_commissioning_aggregations.id_order_mark_commissioning",
	IDOrderMarkAggregation:   "order_mark_commissioning_aggregations.id_order_mark_aggregation",
	Code:                     "order_mark_commissioning_aggregations.code",
	Status:                   "order_mark_commissioning_aggregations.status",
}

// Generated where

var OrderMarkCommissioningAggregationWhere = struct {
	ID                       whereHelperint
	IDOrderMarkCommissioning whereHelpernull_Int
	IDOrderMarkAggregation   whereHelpernull_Int
	Code                     whereHelpernull_String
	Status                   whereHelpernull_String
}{
	ID:                       whereHelperint{field: "[dbo].[order_mark_commissioning_aggregations].[id]"},
	IDOrderMarkCommissioning: whereHelpernull_Int{field: "[dbo].[order_mark_commissioning_aggregations].[id_order_mark_commissioning]"},
	IDOrderMarkAggregation:   whereHelpernull_Int{field: "[dbo].[order_mark_commissioning_aggregations].[id_order_mark_aggregation]"},
	Code:                     whereHelpernull_String{field: "[dbo].[order_mark_commissioning_aggregations].[code]"},
	Status:                   whereHelpernull_String{field: "[dbo].[order_mark_commissioning_aggregations].[status]"},
}

// OrderMarkCommissioningAggregationRels is where relationship names are stored.
var OrderMarkCommissioningAggregationRels = struct {
}{}

// orderMarkCommissioningAggregationR is where relationships are stored.
type orderMarkCommissioningAggregationR struct {
}

// NewStruct creates a new relationship struct
func (*orderMarkCommissioningAggregationR) NewStruct() *orderMarkCommissioningAggregationR {
	return &orderMarkCommissioningAggregationR{}
}

// orderMarkCommissioningAggregationL is where Load methods for each relationship are stored.
type orderMarkCommissioningAggregationL struct{}

var (
	orderMarkCommissioningAggregationAllColumns            = []string{"id", "id_order_mark_commissioning", "id_order_mark_aggregation", "code", "status"}
	orderMarkCommissioningAggregationColumnsWithoutDefault = []string{"id_order_mark_commissioning", "id_order_mark_aggregation", "code", "status"}
	orderMarkCommissioningAggregationColumnsWithDefault    = []string{"id"}
	orderMarkCommissioningAggregationPrimaryKeyColumns     = []string{"id"}
	orderMarkCommissioningAggregationGeneratedColumns      = []string{"id"}
)

type (
	// OrderMarkCommissioningAggregationSlice is an alias for a slice of pointers to OrderMarkCommissioningAggregation.
	// This should almost always be used instead of []OrderMarkCommissioningAggregation.
	OrderMarkCommissioningAggregationSlice []*OrderMarkCommissioningAggregation
	// OrderMarkCommissioningAggregationHook is the signature for custom OrderMarkCommissioningAggregation hook methods
	OrderMarkCommissioningAggregationHook func(context.Context, boil.ContextExecutor, *OrderMarkCommissioningAggregation) error

	orderMarkCommissioningAggregationQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	orderMarkCommissioningAggregationType                 = reflect.TypeOf(&OrderMarkCommissioningAggregation{})
	orderMarkCommissioningAggregationMapping              = queries.MakeStructMapping(orderMarkCommissioningAggregationType)
	orderMarkCommissioningAggregationPrimaryKeyMapping, _ = queries.BindMapping(orderMarkCommissioningAggregationType, orderMarkCommissioningAggregationMapping, orderMarkCommissioningAggregationPrimaryKeyColumns)
	orderMarkCommissioningAggregationInsertCacheMut       sync.RWMutex
	orderMarkCommissioningAggregationInsertCache          = make(map[string]insertCache)
	orderMarkCommissioningAggregationUpdateCacheMut       sync.RWMutex
	orderMarkCommissioningAggregationUpdateCache          = make(map[string]updateCache)
	orderMarkCommissioningAggregationUpsertCacheMut       sync.RWMutex
	orderMarkCommissioningAggregationUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var orderMarkCommissioningAggregationAfterSelectMu sync.Mutex
var orderMarkCommissioningAggregationAfterSelectHooks []OrderMarkCommissioningAggregationHook

var orderMarkCommissioningAggregationBeforeInsertMu sync.Mutex
var orderMarkCommissioningAggregationBeforeInsertHooks []OrderMarkCommissioningAggregationHook
var orderMarkCommissioningAggregationAfterInsertMu sync.Mutex
var orderMarkCommissioningAggregationAfterInsertHooks []OrderMarkCommissioningAggregationHook

var orderMarkCommissioningAggregationBeforeUpdateMu sync.Mutex
var orderMarkCommissioningAggregationBeforeUpdateHooks []OrderMarkCommissioningAggregationHook
var orderMarkCommissioningAggregationAfterUpdateMu sync.Mutex
var orderMarkCommissioningAggregationAfterUpdateHooks []OrderMarkCommissioningAggregationHook

var orderMarkCommissioningAggregationBeforeDeleteMu sync.Mutex
var orderMarkCommissioningAggregationBeforeDeleteHooks []OrderMarkCommissioningAggregationHook
var orderMarkCommissioningAggregationAfterDeleteMu sync.Mutex
var orderMarkCommissioningAggregationAfterDeleteHooks []OrderMarkCommissioningAggregationHook

var orderMarkCommissioningAggregationBeforeUpsertMu sync.Mutex
var orderMarkCommissioningAggregationBeforeUpsertHooks []OrderMarkCommissioningAggregationHook
var orderMarkCommissioningAggregationAfterUpsertMu sync.Mutex
var orderMarkCommissioningAggregationAfterUpsertHooks []OrderMarkCommissioningAggregationHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *OrderMarkCommissioningAggregation) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range orderMarkCommissioningAggregationAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *OrderMarkCommissioningAggregation) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range orderMarkCommissioningAggregationBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *OrderMarkCommissioningAggregation) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range orderMarkCommissioningAggregationAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *OrderMarkCommissioningAggregation) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range orderMarkCommissioningAggregationBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *OrderMarkCommissioningAggregation) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range orderMarkCommissioningAggregationAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *OrderMarkCommissioningAggregation) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range orderMarkCommissioningAggregationBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *OrderMarkCommissioningAggregation) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range orderMarkCommissioningAggregationAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *OrderMarkCommissioningAggregation) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range orderMarkCommissioningAggregationBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *OrderMarkCommissioningAggregation) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range orderMarkCommissioningAggregationAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddOrderMarkCommissioningAggregationHook registers your hook function for all future operations.
func AddOrderMarkCommissioningAggregationHook(hookPoint boil.HookPoint, orderMarkCommissioningAggregationHook OrderMarkCommissioningAggregationHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		orderMarkCommissioningAggregationAfterSelectMu.Lock()
		orderMarkCommissioningAggregationAfterSelectHooks = append(orderMarkCommissioningAggregationAfterSelectHooks, orderMarkCommissioningAggregationHook)
		orderMarkCommissioningAggregationAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		orderMarkCommissioningAggregationBeforeInsertMu.Lock()
		orderMarkCommissioningAggregationBeforeInsertHooks = append(orderMarkCommissioningAggregationBeforeInsertHooks, orderMarkCommissioningAggregationHook)
		orderMarkCommissioningAggregationBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		orderMarkCommissioningAggregationAfterInsertMu.Lock()
		orderMarkCommissioningAggregationAfterInsertHooks = append(orderMarkCommissioningAggregationAfterInsertHooks, orderMarkCommissioningAggregationHook)
		orderMarkCommissioningAggregationAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		orderMarkCommissioningAggregationBeforeUpdateMu.Lock()
		orderMarkCommissioningAggregationBeforeUpdateHooks = append(orderMarkCommissioningAggregationBeforeUpdateHooks, orderMarkCommissioningAggregationHook)
		orderMarkCommissioningAggregationBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		orderMarkCommissioningAggregationAfterUpdateMu.Lock()
		orderMarkCommissioningAggregationAfterUpdateHooks = append(orderMarkCommissioningAggregationAfterUpdateHooks, orderMarkCommissioningAggregationHook)
		orderMarkCommissioningAggregationAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		orderMarkCommissioningAggregationBeforeDeleteMu.Lock()
		orderMarkCommissioningAggregationBeforeDeleteHooks = append(orderMarkCommissioningAggregationBeforeDeleteHooks, orderMarkCommissioningAggregationHook)
		orderMarkCommissioningAggregationBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		orderMarkCommissioningAggregationAfterDeleteMu.Lock()
		orderMarkCommissioningAggregationAfterDeleteHooks = append(orderMarkCommissioningAggregationAfterDeleteHooks, orderMarkCommissioningAggregationHook)
		orderMarkCommissioningAggregationAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		orderMarkCommissioningAggregationBeforeUpsertMu.Lock()
		orderMarkCommissioningAggregationBeforeUpsertHooks = append(orderMarkCommissioningAggregationBeforeUpsertHooks, orderMarkCommissioningAggregationHook)
		orderMarkCommissioningAggregationBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		orderMarkCommissioningAggregationAfterUpsertMu.Lock()
		orderMarkCommissioningAggregationAfterUpsertHooks = append(orderMarkCommissioningAggregationAfterUpsertHooks, orderMarkCommissioningAggregationHook)
		orderMarkCommissioningAggregationAfterUpsertMu.Unlock()
	}
}

// OneG returns a single orderMarkCommissioningAggregation record from the query using the global executor.
func (q orderMarkCommissioningAggregationQuery) OneG(ctx context.Context) (*OrderMarkCommissioningAggregation, error) {
	return q.One(ctx, boil.GetContextDB())
}

// One returns a single orderMarkCommissioningAggregation record from the query.
func (q orderMarkCommissioningAggregationQuery) One(ctx context.Context, exec boil.ContextExecutor) (*OrderMarkCommissioningAggregation, error) {
	o := &OrderMarkCommissioningAggregation{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "znakboil: failed to execute a one query for order_mark_commissioning_aggregations")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all OrderMarkCommissioningAggregation records from the query using the global executor.
func (q orderMarkCommissioningAggregationQuery) AllG(ctx context.Context) (OrderMarkCommissioningAggregationSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// All returns all OrderMarkCommissioningAggregation records from the query.
func (q orderMarkCommissioningAggregationQuery) All(ctx context.Context, exec boil.ContextExecutor) (OrderMarkCommissioningAggregationSlice, error) {
	var o []*OrderMarkCommissioningAggregation

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "znakboil: failed to assign all query results to OrderMarkCommissioningAggregation slice")
	}

	if len(orderMarkCommissioningAggregationAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all OrderMarkCommissioningAggregation records in the query using the global executor
func (q orderMarkCommissioningAggregationQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// Count returns the count of all OrderMarkCommissioningAggregation records in the query.
func (q orderMarkCommissioningAggregationQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "znakboil: failed to count order_mark_commissioning_aggregations rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q orderMarkCommissioningAggregationQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// Exists checks if the row exists in the table.
func (q orderMarkCommissioningAggregationQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "znakboil: failed to check if order_mark_commissioning_aggregations exists")
	}

	return count > 0, nil
}

// OrderMarkCommissioningAggregations retrieves all the records using an executor.
func OrderMarkCommissioningAggregations(mods ...qm.QueryMod) orderMarkCommissioningAggregationQuery {
	mods = append(mods, qm.From("[dbo].[order_mark_commissioning_aggregations]"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"[dbo].[order_mark_commissioning_aggregations].*"})
	}

	return orderMarkCommissioningAggregationQuery{q}
}

// FindOrderMarkCommissioningAggregationG retrieves a single record by ID.
func FindOrderMarkCommissioningAggregationG(ctx context.Context, iD int, selectCols ...string) (*OrderMarkCommissioningAggregation, error) {
	return FindOrderMarkCommissioningAggregation(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindOrderMarkCommissioningAggregation retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindOrderMarkCommissioningAggregation(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*OrderMarkCommissioningAggregation, error) {
	orderMarkCommissioningAggregationObj := &OrderMarkCommissioningAggregation{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from [dbo].[order_mark_commissioning_aggregations] where [id]=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, orderMarkCommissioningAggregationObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "znakboil: unable to select from order_mark_commissioning_aggregations")
	}

	if err = orderMarkCommissioningAggregationObj.doAfterSelectHooks(ctx, exec); err != nil {
		return orderMarkCommissioningAggregationObj, err
	}

	return orderMarkCommissioningAggregationObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *OrderMarkCommissioningAggregation) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *OrderMarkCommissioningAggregation) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("znakboil: no order_mark_commissioning_aggregations provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(orderMarkCommissioningAggregationColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	orderMarkCommissioningAggregationInsertCacheMut.RLock()
	cache, cached := orderMarkCommissioningAggregationInsertCache[key]
	orderMarkCommissioningAggregationInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			orderMarkCommissioningAggregationAllColumns,
			orderMarkCommissioningAggregationColumnsWithDefault,
			orderMarkCommissioningAggregationColumnsWithoutDefault,
			nzDefaults,
		)
		wl = strmangle.SetComplement(wl, orderMarkCommissioningAggregationGeneratedColumns)

		cache.valueMapping, err = queries.BindMapping(orderMarkCommissioningAggregationType, orderMarkCommissioningAggregationMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(orderMarkCommissioningAggregationType, orderMarkCommissioningAggregationMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO [dbo].[order_mark_commissioning_aggregations] ([%s]) %%sVALUES (%s)%%s", strings.Join(wl, "],["), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO [dbo].[order_mark_commissioning_aggregations] %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryOutput = fmt.Sprintf("OUTPUT INSERTED.[%s] ", strings.Join(returnColumns, "],INSERTED.["))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "znakboil: unable to insert into order_mark_commissioning_aggregations")
	}

	if !cached {
		orderMarkCommissioningAggregationInsertCacheMut.Lock()
		orderMarkCommissioningAggregationInsertCache[key] = cache
		orderMarkCommissioningAggregationInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single OrderMarkCommissioningAggregation record using the global executor.
// See Update for more documentation.
func (o *OrderMarkCommissioningAggregation) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// Update uses an executor to update the OrderMarkCommissioningAggregation.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *OrderMarkCommissioningAggregation) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	orderMarkCommissioningAggregationUpdateCacheMut.RLock()
	cache, cached := orderMarkCommissioningAggregationUpdateCache[key]
	orderMarkCommissioningAggregationUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			orderMarkCommissioningAggregationAllColumns,
			orderMarkCommissioningAggregationPrimaryKeyColumns,
		)
		wl = strmangle.SetComplement(wl, orderMarkCommissioningAggregationGeneratedColumns)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("znakboil: unable to update order_mark_commissioning_aggregations, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE [dbo].[order_mark_commissioning_aggregations] SET %s WHERE %s",
			strmangle.SetParamNames("[", "]", 1, wl),
			strmangle.WhereClause("[", "]", len(wl)+1, orderMarkCommissioningAggregationPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(orderMarkCommissioningAggregationType, orderMarkCommissioningAggregationMapping, append(wl, orderMarkCommissioningAggregationPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "znakboil: unable to update order_mark_commissioning_aggregations row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "znakboil: failed to get rows affected by update for order_mark_commissioning_aggregations")
	}

	if !cached {
		orderMarkCommissioningAggregationUpdateCacheMut.Lock()
		orderMarkCommissioningAggregationUpdateCache[key] = cache
		orderMarkCommissioningAggregationUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllG updates all rows with the specified column values.
func (q orderMarkCommissioningAggregationQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values.
func (q orderMarkCommissioningAggregationQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "znakboil: unable to update all for order_mark_commissioning_aggregations")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "znakboil: unable to retrieve rows affected for order_mark_commissioning_aggregations")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o OrderMarkCommissioningAggregationSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o OrderMarkCommissioningAggregationSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("znakboil: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), orderMarkCommissioningAggregationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE [dbo].[order_mark_commissioning_aggregations] SET %s WHERE %s",
		strmangle.SetParamNames("[", "]", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, orderMarkCommissioningAggregationPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "znakboil: unable to update all in orderMarkCommissioningAggregation slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "znakboil: unable to retrieve rows affected all in update all orderMarkCommissioningAggregation")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *OrderMarkCommissioningAggregation) UpsertG(ctx context.Context, updateColumns, insertColumns boil.Columns) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateColumns, insertColumns)
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
func (o *OrderMarkCommissioningAggregation) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("znakboil: no order_mark_commissioning_aggregations provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(orderMarkCommissioningAggregationColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	orderMarkCommissioningAggregationUpsertCacheMut.RLock()
	cache, cached := orderMarkCommissioningAggregationUpsertCache[key]
	orderMarkCommissioningAggregationUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			orderMarkCommissioningAggregationAllColumns,
			orderMarkCommissioningAggregationColumnsWithDefault,
			orderMarkCommissioningAggregationColumnsWithoutDefault,
			nzDefaults,
		)

		insert = strmangle.SetComplement(insert, orderMarkCommissioningAggregationGeneratedColumns)

		for i, v := range insert {
			if strmangle.ContainsAny(orderMarkCommissioningAggregationPrimaryKeyColumns, v) && strmangle.ContainsAny(orderMarkCommissioningAggregationColumnsWithDefault, v) {
				insert = append(insert[:i], insert[i+1:]...)
			}
		}
		if len(insert) == 0 {
			return errors.New("znakboil: unable to upsert order_mark_commissioning_aggregations, could not build insert column list")
		}

		update := updateColumns.UpdateColumnSet(
			orderMarkCommissioningAggregationAllColumns,
			orderMarkCommissioningAggregationPrimaryKeyColumns,
		)

		insert = strmangle.SetComplement(insert, orderMarkCommissioningAggregationGeneratedColumns)

		ret := strmangle.SetComplement(orderMarkCommissioningAggregationAllColumns, strmangle.SetIntersect(insert, update))

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("znakboil: unable to upsert order_mark_commissioning_aggregations, could not build update column list")
		}

		cache.query = buildUpsertQueryMSSQL(dialect, "[dbo].[order_mark_commissioning_aggregations]", orderMarkCommissioningAggregationPrimaryKeyColumns, update, insert, ret)

		whitelist := make([]string, len(orderMarkCommissioningAggregationPrimaryKeyColumns))
		copy(whitelist, orderMarkCommissioningAggregationPrimaryKeyColumns)
		whitelist = append(whitelist, update...)
		whitelist = append(whitelist, insert...)

		cache.valueMapping, err = queries.BindMapping(orderMarkCommissioningAggregationType, orderMarkCommissioningAggregationMapping, whitelist)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(orderMarkCommissioningAggregationType, orderMarkCommissioningAggregationMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // MSSQL doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "znakboil: unable to upsert order_mark_commissioning_aggregations")
	}

	if !cached {
		orderMarkCommissioningAggregationUpsertCacheMut.Lock()
		orderMarkCommissioningAggregationUpsertCache[key] = cache
		orderMarkCommissioningAggregationUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single OrderMarkCommissioningAggregation record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *OrderMarkCommissioningAggregation) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// Delete deletes a single OrderMarkCommissioningAggregation record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *OrderMarkCommissioningAggregation) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("znakboil: no OrderMarkCommissioningAggregation provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), orderMarkCommissioningAggregationPrimaryKeyMapping)
	sql := "DELETE FROM [dbo].[order_mark_commissioning_aggregations] WHERE [id]=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "znakboil: unable to delete from order_mark_commissioning_aggregations")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "znakboil: failed to get rows affected by delete for order_mark_commissioning_aggregations")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q orderMarkCommissioningAggregationQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all matching rows.
func (q orderMarkCommissioningAggregationQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("znakboil: no orderMarkCommissioningAggregationQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "znakboil: unable to delete all from order_mark_commissioning_aggregations")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "znakboil: failed to get rows affected by deleteall for order_mark_commissioning_aggregations")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o OrderMarkCommissioningAggregationSlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o OrderMarkCommissioningAggregationSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(orderMarkCommissioningAggregationBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), orderMarkCommissioningAggregationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM [dbo].[order_mark_commissioning_aggregations] WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, orderMarkCommissioningAggregationPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "znakboil: unable to delete all from orderMarkCommissioningAggregation slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "znakboil: failed to get rows affected by deleteall for order_mark_commissioning_aggregations")
	}

	if len(orderMarkCommissioningAggregationAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *OrderMarkCommissioningAggregation) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("znakboil: no OrderMarkCommissioningAggregation provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *OrderMarkCommissioningAggregation) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindOrderMarkCommissioningAggregation(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *OrderMarkCommissioningAggregationSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("znakboil: empty OrderMarkCommissioningAggregationSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *OrderMarkCommissioningAggregationSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := OrderMarkCommissioningAggregationSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), orderMarkCommissioningAggregationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT [dbo].[order_mark_commissioning_aggregations].* FROM [dbo].[order_mark_commissioning_aggregations] WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, orderMarkCommissioningAggregationPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "znakboil: unable to reload all in OrderMarkCommissioningAggregationSlice")
	}

	*o = slice

	return nil
}

// OrderMarkCommissioningAggregationExistsG checks if the OrderMarkCommissioningAggregation row exists.
func OrderMarkCommissioningAggregationExistsG(ctx context.Context, iD int) (bool, error) {
	return OrderMarkCommissioningAggregationExists(ctx, boil.GetContextDB(), iD)
}

// OrderMarkCommissioningAggregationExists checks if the OrderMarkCommissioningAggregation row exists.
func OrderMarkCommissioningAggregationExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select case when exists(select top(1) 1 from [dbo].[order_mark_commissioning_aggregations] where [id]=$1) then 1 else 0 end"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "znakboil: unable to check if order_mark_commissioning_aggregations exists")
	}

	return exists, nil
}

// Exists checks if the OrderMarkCommissioningAggregation row exists.
func (o *OrderMarkCommissioningAggregation) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return OrderMarkCommissioningAggregationExists(ctx, exec, o.ID)
}
